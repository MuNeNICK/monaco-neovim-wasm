{
  "name": "monaco-neovim-wasm-workspace",
  "version": "0.1.6",
  "private": true,
  "type": "module",
  "description": "Workspace for @monaco-neovim-wasm/* packages.",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "build:assets": "bash ./scripts/build-nvim-assets.sh",
    "build": "npm -w @monaco-neovim-wasm/lib run build && npm -w monaco-neovim-wasm-demo run build",
    "build:all": "npm run build:assets && npm run build",
    "typecheck": "npm -w @monaco-neovim-wasm/lib run typecheck && npm -w monaco-neovim-wasm-demo run typecheck",
    "build:types": "npm -w @monaco-neovim-wasm/lib run build:types"
  },
  "devDependencies": {
    "monaco-editor": "^0.44.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
